import defaultTheme from './default';

export default {
  ...defaultTheme,
  background: 'white',
  commandColor: 'black',
  outputColor: 'black',
  errorOutputColor: '#e856ee',
  promptSymbolColor: '#9156ff'
};
