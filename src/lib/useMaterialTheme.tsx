import {MuiThemeProvider} from "@material-ui/core";
import * as React from 'react';
import {theme} from "../styles/theme";

export const useMaterialTheme = React.useEffect(() => {
  // Execute effect

  return () => {
    // Clean up
  };
}, []);
